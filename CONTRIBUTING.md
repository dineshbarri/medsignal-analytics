# Contributing

Open an issue describing the analytical question or defect before a large change. Use a focused branch, add tests for metric logic, run `ruff check src tests` and `pytest`, then submit a concise pull request. Never commit API keys, patient-identifiable data, or unreviewed clinical claims.
